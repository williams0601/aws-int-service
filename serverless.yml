service: aws-lambda
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-2

functions:
  hello:
    handler: src/index.hello
    events:
      - httpApi:
          path: /
          method: get
